version: "3.9"

services:
  traefik:
    image: traefik:v2.11
    container_name: traefik
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.le.acme.tlschallenge=true"
      - "--certificatesresolvers.le.acme.email=admin@crewdegen.com"
      - "--certificatesresolvers.le.acme.storage=/letsencrypt/acme.json"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/letsencrypt:/letsencrypt
    restart: unless-stopped

  app:
    build: .
    container_name: crewdegen-app
    environment:
      - NODE_ENV=production
      - PORT=3000
      - CREWMIND_API_BASE=https://data.crewmind.xyz
      - BASE_URL=https://crewdegen.com
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.crewdegen.rule=Host(`crewdegen.com`) || Host(`www.crewdegen.com`) || Host(`80.89.237.173`)"
      - "traefik.http.routers.crewdegen.entrypoints=web,websecure"
      - "traefik.http.routers.crewdegen.tls=true"
      - "traefik.http.routers.crewdegen.tls.certresolver=le"
      - "traefik.http.services.crewdegen.loadbalancer.server.port=3000"
    restart: unless-stopped

  db:
    image: mysql:8.0
    container_name: db
    env_file:
      - .env
    environment:
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    volumes:
      - db-data:/var/lib/mysql
    restart: unless-stopped

volumes:
  db-data:
